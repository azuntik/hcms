name: hugo_cms_backend
description: Backend API service for Hugo CMS
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  # HTTP server
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5

  # Utilities
  dotenv: ^4.2.0
  yaml: ^3.1.2
  path: ^1.9.0
  uuid: ^4.3.3

  # Process execution
  process_run: ^0.14.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
